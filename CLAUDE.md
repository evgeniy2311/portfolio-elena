@AGENTS.md

# Claude entry point

Главные инструкции проекта находятся в `AGENTS.md`. Если импорт выше не поддерживается текущим клиентом, открой и полностью прочитай `AGENTS.md`, затем следуй указанному там порядку чтения. Не дублируй правила здесь: это предотвращает расхождение инструкций между Claude и Codex.

